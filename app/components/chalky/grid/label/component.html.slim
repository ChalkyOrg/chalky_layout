.flex.items-center.truncate
  - if has_icon?
    i.mr-1.fas.fa-fw class=("#{icon_class} #{error_class}")
  span class=(error_class) = label
  - if display_required?
    span.text-chalky-text-tertiary class=(error_class) *
